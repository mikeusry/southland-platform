---
/**
 * /referral/ — Redirect to rewards page (which includes referral program).
 *
 * BettyHero (evangelist stage) links here with "Get Your Referral Link" CTA.
 * The referral program lives within the rewards page.
 */
return Astro.redirect('/rewards/', 302)
---
